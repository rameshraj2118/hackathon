import { AuthProvider, useAuth } from './context/AuthContext'
import { Brand } from './components/Brand'
import { LoginForm } from './components/LoginForm'
import './App.css'
function AppContent() { const { user, loading, logout } = useAuth(); return <main className="login-page"><section className="intro" aria-label="AssetNexsus introduction"><div className="intro-content"><Brand light /><div className="intro-copy"><p className="eyebrow">ASSET INTELLIGENCE, CONNECTED</p><h1>Everything your assets need. In one clear view.</h1><p className="lede">Track, manage and get more from every asset across your organisation.</p></div><div className="feature-card"><div className="pulse-icon"><svg viewBox="0 0 24 24"><path d="M4 12h3l2-5 4 10 2-5h5" /></svg></div><div><strong>Built for better decisions</strong><span>Real-time visibility from acquisition to retirement.</span></div></div></div><footer>© 2026 AssetNexsus &nbsp;·&nbsp; Asset management, made simple.</footer></section><section className="signin" id="top"><Brand className="mobile-brand" />{loading ? <p className="loading">Restoring your session…</p> : user ? <section className="signed-in"><p className="eyebrow">SIGNED IN</p><h2>Welcome back, {user.name}.</h2><p>Your AssetNexsus workspace is ready.</p><button className="submit" onClick={logout}>Sign out</button></section> : <div className="form-wrap"><div className="welcome"><p className="eyebrow">WELCOME BACK</p><h2>Sign in to your workspace</h2><p>Enter your details to continue to AssetNexsus.</p></div><LoginForm /><div className="divider"><span>or continue with</span></div><button className="sso" type="button" disabled title="Single sign-on will be connected to your identity provider">Continue with Google</button><p className="request">Need access to AssetNexsus? <a href="#request">Request an account</a></p></div>}<p className="help">Having trouble signing in? <a href="#support">Contact support</a></p></section></main> }
export default function App() { return <AuthProvider><AppContent /></AuthProvider> }
